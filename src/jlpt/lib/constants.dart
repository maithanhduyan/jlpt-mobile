import 'package:flutter/material.dart';

/// Default Background Color
const dBackgroundColor = Color(0xFFF1EFF1);

/// Default Primary Color
const dPrimaryColor = Color(0xFF035AA6);
const dSecondaryColor = Color(0xFFFFA41B);
const dTextColor = Color(0xFF000839);
const dTextLightColor = Color(0xFF747474);
const dBlueColor = Color(0xFF40BAD5);

/// Default Padding
const defaultPadding = 20.0;


// our default Shadow
const defaultShadow = BoxShadow(
  offset: Offset(0, 15),
  blurRadius: 27,
  color: Colors.black12, // Black color with 12% opacity
);

/// Main API URL
/// http://10.0.2.2:8080/api/
const String dApiURL = "http://10.0.2.2:8080/api/";

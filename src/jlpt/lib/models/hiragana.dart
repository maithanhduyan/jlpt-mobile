/// Hiragana Model
class Hiragana {
  final int id;
  final String name;
  final String romaji;
  final String note;
  final String imgURL;

  Hiragana(this.id, this.name, this.romaji, this.note, this.imgURL);
}
